{
  "apiKey": "{{apiKey}}",
  "partnerId": "{{partnerId}}",
  "requestTime": "{{requestTime}}",
  "request": {
    "individualId": "{{individualId}}",
    "authFactorType": "{{authFactorType1}}",
    "format": "{{format1}}",
    "challengeList": [
      {
        "authFactorType": "{{authFactorType}}",
        "challenge": "{{challenge}}",
        "format": "{{format}}"
      }
    ],
    "publicKey": {{publicKey}}
  }
}