definePolicyGroup:
   Esignet_DefinePolicyGroup_All_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$POLICYGROUPDESC$",
    "name": "$POLICYGROUPNAME$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
}'

   Esignet_DefinePolicyGroup_Misp_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$MISPPOLICYGROUPDESC$",
    "name": "$MISPPOLICYGROUPNAME$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
}'

   Esignet_DefinePolicyGroup_in_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$RID$",
    "name": "$RID$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
    }'
   Esignet_DefinePolicyGroup_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: esignet/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$RIDDEL$",
    "name": "$RIDDEL$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
    }'