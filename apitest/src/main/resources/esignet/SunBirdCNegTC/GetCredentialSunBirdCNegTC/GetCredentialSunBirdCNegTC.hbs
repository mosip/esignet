{
	"client_id": "{{client_id}}",
	"idpAccessToken": "{{idpAccessToken}}",
    "format": "{{format}}",
    "credential_definition" : { 
            "type": [
	      {{#each type}}
	            "{{types}}"
	          {{#unless @last}},{{/unless}}
	      	{{/each}}
        ],
            "@context": [
	      {{#each @context}}
	            "{{context}}"
	          {{#unless @last}},{{/unless}}
	      	{{/each}}
        ],
       },
    "proof": {
        "proof_type": "{{proof_type}}",
        "jwt": "{{proof_jwt}}"
    }
}