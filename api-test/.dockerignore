out/
# Never bake a developer's credentials into the image. The tracked per-plugin
# configs under data/config/ are copied in by the Dockerfile; these two are the
# local, secret-bearing ones.
data/config/config.local.json
data/config/config.json
conformance-suite-private/
*esignet-config*.json
api/**/*.test
.env
.git
