{
    "clientId": "{{clientId}}",
    "requestTime": "{{requestTime}}",
    "request": {
        "encPublicKey": {
            "kty": "{{kty}}",
            "e": "{{e}}",
            "use": "{{use}}",
            "kid": "{{kid}}",
            "alg": "{{alg}}",
            "n": "{{n}}"
        }
    }
}
