{
  "headerTransactionID": "{{headerTransactionID}}",
  "requestTime": "{{requestTime}}",
  "request": {
    "identifier": "{{identifier}}",
    "challengeInfo": [{
      "challenge": "{{challenge}}",
      "format": "{{format}}",
      "type": "{{type}}"
    }]
  }
}