{
	"offset": {{offset}},
	"limit": {{limit}},
	"filters": {
		{{#if fullName}}
		"fullName": {
			"eq": "{{fullName}}"
		},
		{{/if}}
		{{#if dob}}
		"dob": {
			"eq": "{{dob}}"
		},
		{{/if}}
		{{#if policyNumber}}
		"policyNumber": {
			"eq": "{{policyNumber}}"
		}
		{{/if}}
	}
}
