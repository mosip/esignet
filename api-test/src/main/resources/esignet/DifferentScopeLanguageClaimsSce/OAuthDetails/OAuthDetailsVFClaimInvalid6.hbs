{
    "requestTime": "{{requestTime}}",
    "request": {
        "clientId": "{{clientId}}",
        "scope": "{{scope}}",
        "responseType": "{{responseType}}",
        "redirectUri": "{{redirectUri}}",
        "display": "{{display}}",
        "prompt": "{{prompt}}",
        "acrValues": "{{acrValues}}",
        "claims": {
            "userinfo": {
                "verified_claims": [
                    {
                        "verification": {
                            "trust_framework": {
                                "values": [
                                    "eidas"
                                ]
                            },
                            "verification_process": "manual_verification",
                            "max_age": null
                        },
                        "claims": {
                            "phone_number": {
                                "essential": true
                            },
                            "name": {
                                "essential": true
                            }
                        }
                    }
                ]
            },
            "id_token": {}
        },
        "nonce": "{{nonce}}",
        "state": "{{state}}",
        "claimsLocales": "{{claimsLocales}}"
    }
}