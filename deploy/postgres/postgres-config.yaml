
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: postgres
  labels:
    app: postgres
data:
  database-host: "postgres-postgresql"
  database-port: "5434"
  database-username: "esignet"
  database-name: "mosip_esignet"
