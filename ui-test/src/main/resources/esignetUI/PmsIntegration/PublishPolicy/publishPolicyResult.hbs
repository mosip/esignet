{
    "response": {
        "policyGroupStatus": "{{policyGroupStatus}}",
        "policyType": "{{policyType}}",
        "is_Active": "{{is_Active}}",
        "policies": {
            "allowedAuthTypes": [
                {
                    "authType": "{{authType}}"
                }
            ]
        }
    }
}