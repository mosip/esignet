{
    "requestTime": "{{requestTime}}",
    "request": {
        "clientId": "{{clientId}}",
        "clientName": "{{clientName}}",
        "publicKey": {{publicKey}},
        "relyingPartyId": "{{relyingPartyId}}",
        "userClaims": [
            "{{userClaims1}}",
            "{{userClaims2}}",
            "{{userClaims3}}",
            "{{userClaims4}}",
            "{{userClaims5}}"
        ],
        "authContextRefs": [
		      {{#each authContextRefs}}
		            "{{acrValues}}"
		          {{#unless @last}},{{/unless}}
		      	{{/each}}
        ],
        "logoUri": "{{logoUri}}",
        "redirectUris": [
          "{{redirectUris}}"
  ],
        "grantTypes": [
            "{{grantTypes}}"
        ],
        "clientAuthMethods": [
            "{{clientAuthMethods}}"
        ],
    	"clientNameLangMap": {
		      "{{keyLang1}}": "{{clientNameLang}}"{{#if keyLang2}},
		      "{{keyLang2}}": "{{clientNameLang2}}"{{/if}}{{#if keyLang3}},
		      "{{keyLang3}}": "{{clientNameLang3}}"{{/if}}{{#if clientNameLangNone}},
		      "@none": "{{clientNameLangNone}}"{{/if}}
    	},
    	
    "additionalConfig": {
      "userinfo_response_type": "{{userinfo_response_type}}",
      "purpose": {
        "title": {
          "@none": {{#if purpose_title_is_null}}null{{else}}"{{purpose_title}}"{{/if}}
        },
        "type": "{{purpose_type}}",
        "subTitle": {
          "@none": {{#if purpose_subTitle_is_null}}null{{else}}"{{purpose_subTitle}}"{{/if}}
        }
      },
      "signup_banner_required": {{signup_banner_required}},
      "forgot_pwd_link_required": {{forgot_pwd_link_required}},
      "consent_expire_in_mins": {{consent_expire_in_mins}}
    }
    
  }
}